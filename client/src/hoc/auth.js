import React, {useEffect} from 'react';

export default function (SpecificComponents, option, adminRoute = null) {

    // Auth가 Back-end로 Request를 날린다.
    function AuthenticationCheck(props) {

        useEffect(() => {

            //Axios
        })

    }

    return AuthenticationCheck
}